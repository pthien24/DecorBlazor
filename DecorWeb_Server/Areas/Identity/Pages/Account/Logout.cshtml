@page
@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> _signManger

@functions {
    public async Task<IActionResult> OnGet()
    {
        if (_signManger.IsSignedIn(User))
        {
            await _signManger.SignOutAsync();
        }
        return Redirect("~/");
    }
    }